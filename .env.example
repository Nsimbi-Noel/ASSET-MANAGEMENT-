# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Database (default: ./data/database.db)
DB_PATH=

# SSL/TLS (optional — set both for HTTPS)
SSL_KEY_PATH=
SSL_CERT_PATH=

# Rate Limiting (login endpoint)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_LOGIN=10
